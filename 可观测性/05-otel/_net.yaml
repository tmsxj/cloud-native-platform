apiVersion: v1
kind: Pod
metadata:
  name: netcheck
  namespace: monitoring
spec:
  restartPolicy: Never
  containers:
  - name: c
    image: 192.168.1.61/library/alpine_curl
    command: ["sh", "-c"]
    args:
    - |
      curl -s -m 8 -o /dev/null -w "pypi=%{http_code}\n" https://pypi.org; echo
